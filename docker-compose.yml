services:
  frontend:
    image: frontend:1.0
    container_name: react_app
    # networks:
    #   - transcendence
    build:
      context: $FRONTEND
      dockerfile: Dockerfile
    restart: always
    ports:
      - "5173:5173"
    env_file:
      - .env